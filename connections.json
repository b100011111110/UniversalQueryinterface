{
  "postgresql": {
    "url": "${POSTGRES_URL}",
    "userid": "${POSTGRES_USER}",
    "password": "${POSTGRES_PASSWORD}"
  },
  "neo4j": {
    "url": "${NEO4J_URL}",
    "userid": "${NEO4J_USER}",
    "password": "${NEO4J_PASSWORD}"
  },
  "influxdb": {
    "url": "${INFLUXDB_URL}",
    "userid": "${INFLUXDB_ORG}",
    "password": "${INFLUXDB_TOKEN}"
  },
  "chromadb": {
    "url": "${CHROMADB_URL}",
    "userid": "${CHROMADB_USER}",
    "password": "${CHROMADB_PASSWORD}"
  }
}
